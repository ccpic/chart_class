# Docker 环境变量配置示例
# 复制此文件为 .env 并修改相应值
#
# ========== 生产环境必须设置 ==========
#
# JWT 秘钥（必须设置，否则后端无法启动）
# 生成方法：openssl rand -hex 32 或 python -c "import secrets; print(secrets.token_hex(32))"
# 警告：生产环境必须使用强随机秘钥，不要使用默认值！
JWT_SECRET_KEY=your-secret-key-here-change-in-production

# 前端 API 地址（浏览器访问后端时使用）
# 开发环境：http://localhost:8001
# 生产环境：必须设置为实际可访问的地址，例如：
#   - 如果使用域名：https://your-domain.com/api
#   - 如果使用 IP：http://your-server-ip:8001
# 警告：不能使用 localhost（除非仅本地访问），否则浏览器无法访问
NEXT_PUBLIC_API_URL=http://localhost:8001

# ========== 可选配置 ==========

# 后端端口
BACKEND_PORT=8001

# 前端端口
FRONTEND_PORT=3000

# CORS 允许的来源（逗号分隔）
# 生产环境必须设置实际的前端域名
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:5173

# 日志级别
LOG_LEVEL=INFO


